Visualization of invariants for every label.
Invariant with support percentage > 0.1 have been visualized.
Results show that for the same label, there can be more than 1 invariants; each of which use a different logic to make the classification decision.
